- using global configuration : makes changes with the same email id
- using local configuration : making changes in a repo using some other email id

  git config --global user.name "my name"
  git config --global user.email "my@email.com"
  git config --list
